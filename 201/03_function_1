// 1. 반환값(return)이 있는 함수
// 1-1. 2개의 정수 값 더하기
int add1(int x, int y) {
  int result = x + y;
  return result;
 }
 
 // 2. 반환값(return)이 없는 함수
 // 2-1. 2개의 정수 값 더하기
 void add2(int x, int y) {
  int result = x + y;
  print(result);
 }
 
 void main() {
  // add1 함수 호출
  int sum = add1(4, 5);
  print(sum);
  
  // add2 함수 호출
  add2(5, 8);
 }
 
